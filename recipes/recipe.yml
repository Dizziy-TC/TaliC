# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
name: talic
description: Hochmodernes Atomic Gaming- & Streaming-OS (KDE Plasma + NVIDIA).

base-image: ghcr.io/ublue-os/kinoite-nvidia
image-version: 43

modules:
  # Deine Dateien (Logo, ASCII, Fastfetch-Config)
  # Kopiert alles aus 'files/usr' nach '/usr' und 'files/etc' nach '/etc'
  - type: files
    files:
      - source: usr
        destination: /usr
      - source: etc
        destination: /etc

  # Das Logo beim Hochfahren (Startup Splash)
  - type: plymouth
    data:
      theme: bgrt

  # System-Layer (Treiber, Hardware-Tools & Fastfetch)
  - type: rpm-ostree
    repos:
      - https://mirrors.rpmfusion.org/free/fedora/rpmfusion-free-release-43.noarch.rpm
      - https://mirrors.rpmfusion.org/nonfree/fedora/rpmfusion-nonfree-release-43.noarch.rpm
    install:
      - fastfetch
      - mangohud
      - gamemode
      - obs-studio
      - obs-studio-plugin-vaapi
      - pipewire-utils
      - nvtop
      - btop

  # App-Layer (Isoliert & Sicher als Flatpaks)
  - type: default-flatpaks
    configurations:
      - install:
          - com.valvesoftware.Steam
          - net.lutris.Lutris
          - org.mozilla.firefox
          - com.discordapp.Discord
          - org.kde.kcalc
          - net.davidotek.pupgui2

  # Scripte (Hier wird unter anderem dein Startmenü-Icon aktiviert)
  - type: script
    scripts:
      - post-install.sh
